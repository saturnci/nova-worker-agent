#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../../lib/executor'
require_relative '../../lib/adapters/rails_rspec/worker'

executor = Executor.new
worker = Adapters::RailsRSpec::Worker.new(executor)
worker.run
