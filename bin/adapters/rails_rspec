#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../../lib/executor'

executor = Executor.new
executor.start_stream
puts 'Rails/RSpec adapter running...'

executor.clone_repo

puts 'Run finished.'
executor.kill_stream
